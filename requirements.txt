# Core dependencies
gspread>=5.7.0
google-auth>=2.16.0
google-auth-oauthlib>=0.8.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.70.0
google-oauth2-service-account>=0.1.0

# Video/Audio processing
moviepy>=1.0.3
ffmpeg-python>=0.2.0

# HTTP requests
requests>=2.28.0

# Optional AI integrations
openai>=1.0.0

# Date/time utilities
python-dateutil>=2.8.0

# JSON handling (usually built-in, but explicit for clarity)
# json - built-in module

# Additional utilities
Pillow>=9.0.0  # For image processing if needed
numpy>=1.21.0  # Required by moviepy
decorator>=4.4.0  # Required by moviepy
imageio>=2.4.0  # Required by moviepy
imageio-ffmpeg>=0.4.0  # Required by moviepy
proglog>=0.1.9  # Required by moviepy
tqdm>=4.64.0  # Required by moviepy
